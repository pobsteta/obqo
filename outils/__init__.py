"""Outils du depot : ce qui sert a fabriquer obqo, pas a calepiner.

Ce paquet n'est pas livre dans la roue (`tool.hatch.build` ne prend que
`src/obqo`). Il est teste et type comme le reste : une chaine de publication
qui se trompe de version coute plus cher qu'un bug d'affichage.
"""
